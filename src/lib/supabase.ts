// Stub to route data queries through the unified database loader

export const fetchConfigSynchronously = () => {};

export const getSupabaseClient = (forceRefresh?: boolean) => {
  return null;
};

export const isSupabaseEnabled = (): boolean => {
  return true;
};
